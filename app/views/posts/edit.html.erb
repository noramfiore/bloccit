<h1>Edit Post</h1>
<div class="row">
  <div class="cold-md-4">
    <p>Guidelines for posts</p>
    <ul>
      <li>Make sure the it rhymes.</li>
      <li>Don't use the letter "A".</li>
      <li>The incessant use of hastags will get you banned.</li>
    </ul> 
  </div>
  <div class="col-md-8">
    <%= form_for @post do |f|%>
      <div class="form-group">
        <%= f.label :title %>
        <%= f.label :text_field, class: "form-control", placeholder: "Enter post title" %>
      </div>
      <div class="form-group">
        <%= f.label :body %>
        <%= f.label :text_area, class: "form-control", placeholder: "Enter post body" %>
      </div>
      <div class="form-group">
        <%= f.submit "Save", class: 'btn btn-success' %>
      </div>
    <% end %>
  </div>
</div>
